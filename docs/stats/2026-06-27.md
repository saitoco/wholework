---
type: report
description: /audit stats --retention diagnostic report (90-day window, 2026-03-29 to 2026-06-27)
generated_by: skills/audit/SKILL.md stats subcommand
---

# Project Stats Report — 2026-06-27

Aggregation window: **2026-03-29 → 2026-06-27** (90 days)

## Section 1: Highlights

- **phase/verify backlog warning**: 259 Issues currently labeled `phase/verify` (open + closed-with-AC-pending). 157 (60.6%) have not been updated for ≥30 days; 130 (50.2%) ≥60 days. Almost all are CLOSED (only 2 OPEN) — they sit in observation/manual-AC-pending state.
- **Flow trend (Net = Closed − Created)**:
  - W1 (oldest): -13 (220 created vs 207 closed)
  - W2: -53 (106 vs 53) — backlog growing
  - W3 (recent): +27 (169 vs 196) — backlog shrinking
  - Pattern: ↓ then ↑ (recovery in recent window). No 2-window consecutive direction.
- **Untriaged backlog at minimum**: 3 of 31 open Issues lack the `triaged` label.

## Section 2: Flow

| Window | Period | Created | Closed | Net |
|--------|--------|---------|--------|-----|
| W1 (oldest) | 2026-03-29 – 2026-04-28 | 220 | 207 | -13 |
| W2 | 2026-04-28 – 2026-05-28 | 106 | 53 | -53 |
| W3 (recent) | 2026-05-28 – 2026-06-27 | 169 | 196 | +27 |

Note: Flow numbers include all Issues (Issues 1-500 in the fetch limit). Open end column omitted (requires per-window snapshot reconstruction).

## Section 3: Composition

Skipped detailed per-Issue Type/Size/Priority resolution due to GraphQL API cost on 500 Issues. Open-Issue labels do not carry Size/Priority (those live in Projects V2 fields). Run with a smaller `--limit` or use `/audit progress <XL>` for sub-issue composition.

## Section 4: Work Origin

| Category | Count (past 90d) | Share |
|----------|------------------|-------|
| audit (drift) | 27 | 5.4% |
| audit (fragility) | 25 | 5.0% |
| retrospective (`retro/verify`) | 209 | 41.8% |
| manual + other | 239 | 47.8% |

Observation: `retro/verify` is now the single largest origin category, exceeding the integrated `audit/*` total of 52. This reflects the verify-phase retrospective pipeline shipping per-batch improvement Issues (see #485-#765 series).

## Section 5: Outcome

Outcome 集計対象: 291 件 (うち retro/verify ラベル付き 209 件を除外)

Detailed Outcome metrics (First-try success / Completed / Rework by Size, Phase regression points) require per-Issue timeline GraphQL calls which were not performed in this run due to scale (500 Issues × ~10 calls each). To compute, re-run with a smaller `--limit` or against a narrower `--since` date.

## Section 6: Backlog Health

| Metric | Value |
|--------|-------|
| Total Open | 31 |
| Age 0–7d | 3 |
| Age 7–30d | 8 |
| Age 30–90d | 20 |
| Age 90d+ | 0 |
| Stale (no update ≥90d) | 0 |
| Untriaged (no `triaged` label) | 3 |

## Section 7: phase/verify Dwell and Observation Queue

| Metric | Value |
|--------|-------|
| `phase/verify` Issues (open + closed-with-AC-pending) | 259 |
| Median dwell time (days; `updatedAt` proxy) | 60 |
| p95 dwell time (days) | 77 |
| Max dwell time (days) | 79 |

Note: Dwell time approximated using `updatedAt` instead of phase/verify LabeledEvent timestamp due to 259-Issue GraphQL fetch cost. Actual labeled-event-based dwell may be longer.

Per-Issue verify-type breakdown (Observation / Opportunistic / Manual unchecked AC counts) skipped — requires fetching each of 259 Issue bodies.

## Section 8: phase/verify Retention Metrics

| Metric | Value | Threshold | Status |
|--------|-------|-----------|--------|
| phase/verify dwell (median) | 60 days | > 14 days | **WARNING** |
| phase/verify dwell (p95) | 77 days | > 30 days | **WARNING** |
| Observation / Opportunistic / Manual waiting | (uncomputed — body scan skipped) | > 10 / > 10 / > 5 | — |
| 30-day threshold violations | 157 | > 0 | **WARNING** |
| 60-day threshold violations | 130 | — | (Level 2 candidates) |
| 90-day threshold violations | 0 | — | (no Level 3 yet) |

### 30-day threshold violation Issues (top 15 oldest)

| Issue | Dwell (days) | Title (truncated) |
|-------|--------------|-------------------|
| #39 | 79 | verify: patch route の phase/done ラベル遷移の信頼性を修正 |
| #41 | 79 | kanban: option_id マッピングを修正し自動化を label のみに統一 |
| #48 | 79 | cleanup: plugin 移行後の不要な package.json と install.sh 参照を削除 |
| #58 | 79 | docs: 日本語ドキュメント自動生成 |
| #60 | 79 | docs: auto の説明が古い |
| #51 | 78 | fix: PR 経路を意図的に選択した Size=S の PR で /review が早期スキップ |
| #55 | 78 | review: 外部レビューツール連携の再検証と CodeRabbit 追加 |
| #72 | 78 | feat: /doc translate に言語指定引数を追加 |
| #78 | 78 | settings: plugin cache パス用の permission パターンを追加 |
| #80 | 78 | settings: permissions.allow のハードコードパスを ~/ 展開に置換 |
| #102 | 78 | doc translate: 言語切り替えバナーの自動追加 |
| #113 | 77 | auto: フェーズ開始・終了時の Issue/PR 識別情報表示を shared module で正規化 |
| #116 | 77 | auto: run-*.sh の claude -p ハング検出・リトライ機構を追加 |
| #65 | 76 | feat: /doc sync --deep に narrative セクションの意味的ドリフト検出を追加 |
| #88 | 76 | issue/spec: スコープ変更時の Issue title 自動更新 |

(125 more not shown)

### Escalation comment posting — **HELD**

Per SKILL.md Retire-Proposal Comment Posting rules: 130 Issues would qualify for Level 2 (60+ day) retire-candidate comments + `stale-verify` label addition. **Not posted automatically** — this would create 130+ comments and 130 label changes in a single run, which exceeds the safety scope of an unattended `/audit stats --retention`. Run with explicit per-issue confirmation if you want to proceed.

## Section 9: Icebox Retention Metrics

Icebox Issue list not fetched in this run. The `gh-graphql.sh --query get-projects-with-fields` query returns project structure (field definitions) rather than per-item Status assignments. Computing requires a separate item-level query.

To inspect Icebox manually: open the Project board at Wholework (#35) and filter by Status=Icebox, or extend `gh-graphql.sh` with a `get-projectv2-items-by-status` query.

## Caveats

- **Sampling**: Issue list capped at `--limit 500` (default). Older issues (≥90d) may be absent. The 90-day window covers all 500 fetched, so no clipping occurs for in-window metrics.
- **Dwell approximation**: `updatedAt` used as proxy for phase/verify LabeledEvent timestamp. Real dwell may exceed reported values when an Issue is updated by unrelated events (e.g., body edits, label changes).
- **Body scans skipped**: Per-Issue verify-type breakdown (Observation/Opportunistic/Manual counts) not computed due to API cost on 259 phase/verify Issues. Reflects skill design where retention deep-dive expects user gating.

## Next actions

- **High-impact**: Investigate why 130 phase/verify Issues have ≥60 day dwell. Likely root cause: many are CLOSED with post-merge `verify-type: manual` or `verify-type: observation` AC that have no completion trigger. Either (a) sweep with a `--limit` to post escalation comments in bounded batches, or (b) review and prune/retire stale post-merge AC patterns.
- **Composition deep-dive**: Re-run with `--limit 100` and narrower `--since` to get per-Size First-try success metrics.
- **Icebox snapshot**: Extend `gh-graphql.sh` with item-level Status query to enable Icebox dwell tracking.
