# Issue #694: audit: batch/XL 完了時の auto-session retrospective (L3) を自動化 + docs/sessions/ レイアウト導入

## Overview

`/auto --batch` または XL 完了時に L3 session retrospective を自動 trigger する。notable な cross-cutting observation がある場合のみ `docs/sessions/{SID}-{DATE}/` を生成し、`modules/retro-proposals.md` 経由で改善 Issue を起票する。単一 `/auto N` (M/L/patch) では L3 は不要 (L4 per-Issue retrospective と重複するため skip)。

## Changed Files

- `skills/auto/SKILL.md`: Step 5 の "Daily rollup" の直後に L3 auto-retrospective block を追加 — bash 3.2+ compatible
- `docs/structure.md`: Directory Layout に `docs/sessions/` エントリを追加
- `docs/ja/structure.md`: 英語 `docs/structure.md` の変更を日本語 mirror に反映

## Implementation Steps

1. `skills/auto/SKILL.md` Step 5 の "Daily rollup" 直後に **L3 auto-retrospective** block を追加 (→ AC 1, 2, 3, 4, 5)

   ```markdown
   **L3 auto-retrospective (batch/XL routes only):**

   1. **Route guard**: If `ROUTE` is neither `batch` nor `XL`, output
      "L3 retrospective skipped: no notable orchestration content" and skip.

   2. **Notable judgment** (using events from `.tmp/auto-events.jsonl` and in-context variables):
      - Extract events for this session:
        ```bash
        jq -c 'select(.session_id == "'"$AUTO_SESSION_ID"'")' .tmp/auto-events.jsonl 2>/dev/null
        ```
      - **batch route** — notable if ANY of:
        - Tier 2/3 recovery fired (`TIER3_RECOVERY_PHASE` is set, OR recovery event detected in events)
        - Verify FAIL (any Issue label `phase/verify` with unchecked `- [ ]` at batch end)
        - Commit count for this session >= 3 (`commit` events in filtered events)
        - Watchdog kill detected (`watchdog_timeout` event in filtered events)
      - **XL route** — notable if ANY of:
        - Parallel race detected (conflicting commit event or explicit race event)
        - Cross-cutting AC mismatch (any `FAIL` from Step 4's cross-cutting pre-verification)
        - At least 1 sub-issue failure in the execution summary
      - If NOT notable: output "L3 retrospective skipped: no notable orchestration content" and skip.

   3. **Create session files**:
      - Compute `DATE=$(date -u +%Y-%m-%d)` and set `SESSION_DIR="docs/sessions/${AUTO_SESSION_ID}-${DATE}"`
      - `mkdir -p "$SESSION_DIR"`
      - Write `$SESSION_DIR/session.md` using the Write tool — format:
        ```markdown
        # L3 Session Retrospective: {AUTO_SESSION_ID}

        ## What worked
        (successful phases, recovery patterns used)

        ## Limits and gaps
        (cross-cutting conflicts, concurrent commit issues, AC mismatches, tier gaps)

        ## Improvement candidates
        (proposals for structural improvements)

        ## Auto Retrospective
        ### Improvement Proposals
        (retro-proposals-compatible section; same content as Improvement candidates above)
        ```
      - Extract session events to `$SESSION_DIR/events.jsonl`:
        ```bash
        jq -c 'select(.session_id == "'"$AUTO_SESSION_ID"'")' .tmp/auto-events.jsonl > "$SESSION_DIR/events.jsonl" 2>/dev/null || true
        ```

   4. **Call `modules/retro-proposals.md`** (→ improvement Issue creation):
      - Create a bridge file for retro-proposals.md interface compatibility:
        - XL route: `BRIDGE_NUMBER=$NUMBER`; write bridge file at
          `$SESSION_DIR/issue-${BRIDGE_NUMBER}-l3session.md` containing the
          `## Auto Retrospective > ### Improvement Proposals` section from `session.md`
        - batch route: `BRIDGE_NUMBER="batch-${AUTO_SESSION_ID}"`;
          write same bridge file at `$SESSION_DIR/issue-${BRIDGE_NUMBER}-l3session.md`
      - Read `${CLAUDE_PLUGIN_ROOT}/modules/retro-proposals.md` and follow "Processing Steps"
        with `SPEC_PATH=$SESSION_DIR`, `NUMBER=$BRIDGE_NUMBER`,
        `HAS_SKILL_PROPOSALS` (already retained).
      - Collect filed Issue numbers from retro-proposals output.
      <!-- TODO (#710): When blocked-by workflow lands, pass BLOCKED_BY_CANDIDATES
           to retro-proposals.md to enable formal relationship setting. -->

   5. **Backlink**: If any Issues were filed, append a `## Filed Issues` section to
      `$SESSION_DIR/session.md` listing each filed Issue number.

   6. **Commit and push**:
      ```bash
      git add "$SESSION_DIR"
      git commit -s -m "Add L3 session retrospective for session ${AUTO_SESSION_ID}

   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
      git push origin main
      ```
   ```

2. `docs/structure.md` Directory Layout ツリーの `docs/` 配下に `docs/sessions/` エントリを追加 (→ SHOULD: 新 output ディレクトリ)

   ```
       └── sessions/            # L3 session retrospectives (generated by /auto Step 5 L3 trigger; notable batch/XL only)
             {SID}-{DATE}/
               session.md       # L3 narrative (What worked / Limits and gaps / Improvement candidates)
               events.jsonl     # session-scoped events extracted from .tmp/auto-events.jsonl
   ```

   追加位置: `docs/stats/` エントリの直後。

3. `docs/ja/structure.md` に英語 `docs/structure.md` と同等の `docs/sessions/` エントリを日本語で反映 (→ SHOULD: translation sync)

## Verification

### Pre-merge

- <!-- verify: grep "L3 auto-retrospective|docs/sessions" "skills/auto/SKILL.md" --> `skills/auto/SKILL.md` に L3 auto-trigger ステップが追加されている
- <!-- verify: grep "retro-proposals" "skills/auto/SKILL.md" --> `skills/auto/SKILL.md` で retro-proposals module 呼び出しが記述されている
- <!-- verify: rubric "skills/auto/SKILL.md Step 5 の L3 auto-trigger が以下を含む: (a) batch/XL route 限定の guard、(b) notable 判定基準 (concurrent commits >= 3件 を含む)、(c) docs/sessions/{SID}-{DATE}/ への出力、(d) retro-proposals module 呼び出し、(e) skip 条件と出力メッセージ" --> rubric 基準を満たす
- <!-- verify: section_contains "skills/auto/SKILL.md" "### Step 5:" "docs/sessions" --> Step 5 セクションに docs/sessions パスが含まれる
- <!-- verify: grep "skip|no notable" "skills/auto/SKILL.md" --> skip 条件 (#436 retrospective skip と類似) が明記されている
- <!-- verify: command "bats tests/auto.bats" --> 既存 bats テストが green
- <!-- verify: github_check "gh pr checks" "Run bats tests" --> bats テストが CI で green

### Post-merge

- 次回 batch /auto 完走時に notable がある場合 `docs/sessions/{SID}-{DATE}/session.md` が生成され、retro-proposals 経由で改善 Issue が起票される (もしくは notable なしで skip ログが出る) ことを確認 <!-- verify-type: observation event=auto-run -->

## Notes

### Auto-Resolved Ambiguity Points (non-interactive mode)

Issue 本文に `## Auto-Resolved Ambiguity Points` セクションとして記録済み。Spec での再掲:

1. **concurrent commits >= X件 の X**: X = 3 を採用。`AUTO_MAX_CONCURRENT` デフォルト 5 に対し、3 件以上の同時コミットは重複リスクを示す合理的な閾値。
2. **SID の形式**: `{PID}-{EPOCH}` 形式 (`AUTO_SESSION_ID`) — Step 1 の `SESSION_ID="$$-$(date +%s)"` と一致。
3. **retro-proposals.md シグネチャ変更スコープ**: 本 Issue 外 (#710 で対応)。現 interface をそのまま使い、設計コメントを SKILL.md に残す。

### retro-proposals.md Interface Bridge

`retro-proposals.md` は `Glob with $SPEC_PATH/issue-$NUMBER-*.md` で Spec ファイルを特定する。L3 session retrospective は通常の Spec ファイル形式ではないため、bridge file を `$SESSION_DIR/issue-{BRIDGE_NUMBER}-l3session.md` として作成し、SPEC_PATH を `$SESSION_DIR` に一時設定してから呼び出す。BRIDGE_NUMBER は XL の場合は親 Issue 番号、batch の場合は `"batch-{AUTO_SESSION_ID}"` (文字列) を使用する。重複チェック (`gh issue list`) はルーティング後の Issue タイトル比較で機能するため、BRIDGE_NUMBER が数値でなくても問題ない。

### Route Detection for ROUTE Variable

Step 5 の L3 guard は Step 2/3 で確定した `ROUTE` 変数を参照。batch mode は Step 1 で `--batch` flag 検出後に Steps 2-6 をスキップするため、batch 完了報告後に L3 block を呼ぶ際は `ROUTE="batch"` を明示的にセットしておく必要がある (Implementation Step 1 内に記載)。

### Commit Count Detection

"concurrent commits >= 3件" の判定は、events ログから `event_type == "commit"` (または類似) のエントリ数をカウントする。events.jsonl フォーマットが commit イベントを emit しない場合は、`git log --oneline --since="$session_start"` の行数を代替として使用する。Implementation 時に events log schema を確認して適切な検出方法を選択すること。

### docs/sessions/ Migration

既存の `docs/reports/auto-session-*.md` は旧 `/audit auto-session` の手動レポート。migration は別 Issue で扱う (Issue 本文に明記)。

### #704 / #710 統合設計コメント

- #704 (autonomy tier): 実装時は route guard のみ (`batch || XL`) で L3 を trigger。autonomy 判定は混ぜない。#704 着地後に 1 行 guard を追加するだけで済む形にしておく。
- #710 (blocked-by workflow): SKILL.md の retro-proposals 呼び出し箇所に `<!-- TODO (#710) -->` コメントを残す。正式 relationship 設定への切り替えは #710 で行う。

## Code Retrospective

### Deviations from Design

- Spec では route guard を `ROUTE == "XL"` と記述していたが、SKILL.md の既存 Step 3a route table では XL は `sub_issue` を使用するため、guard 条件を `ROUTE == "sub_issue"` に変更した。"XL" は Spec の意図する名称で `sub_issue` が実際の変数値であり整合性を優先した。
- Spec では deprecated term の修正スコープは記載されていなかったが、既存 Spec Notes に含まれていたため /code フェーズ内で修正した。

### Design Gaps/Ambiguities

- Spec の "Route Detection for ROUTE Variable" Notes では `ROUTE="batch"` を Batch Completion Report で設定する必要があると述べているが、L3 block の配置場所として Step 5 のみ指示されていた。batch mode は Steps 2-6 をスキップするため Batch Completion Report に L3 block を複製追加 (Event scan + Daily rollup + ROUTE="batch" + L3 参照) した。Spec の実装意図と一致している。
- Spec の Implementation Step 1 コードサンプルに `git push origin main` が含まれていたが、セッション内の commit/push pattern は retrospective commit 時に SKILL.md 外で行われるため、push はコードフェンス内のサンプルとして残しコメントなし。

### Rework

- Spec ファイルの deprecated term 修正後に `git status` で未コミット変更として残り、PR 作成後に追加コミットを要した。spec フェーズが作成した内容の CI scan 対象への該当をコード実装前に確認すべきだった。

## Phase Handoff
<!-- phase: code -->

### Key Decisions
- XL route の guard 条件を Spec の "XL" から実際の SKILL.md 変数値 `sub_issue` に変更。SKILL.md Step 3a route table との整合性を優先。
- Batch Completion Report に L3 block を複製追加 (Event scan + Daily rollup + ROUTE="batch" + L3 参照形式)。Spec が "Step 5 に配置" と述べているが batch は Steps 2-6 をスキップするため必要な対処。
- forbidden expression fix をコード実装後に発見し追加コミットで対応。PR #711 に 2 コミット含まれる。

### Deferred Items
- Commit count detection の実装方法: Spec Notes で "events log schema を確認して適切な検出方法を選択" と指示されていたが、`commit` イベントが存在しない場合の fallback (`git log --oneline --since=...`) として SKILL.md に記載済み。runtime での確認は /verify フェーズ後の手動観察に委ねる。
- `BLOCKED_BY_CANDIDATES` シグネチャ: `<!-- TODO (#710) -->` コメントを SKILL.md に残した。#710 着地まで deferred。
- #704 (autonomy tier) guard: route guard のみで実装済み。#704 着地後の 1 行追加で完結する設計。

### Notes for Next Phase
- PR #711 の CI (bats tests) が green になることを確認してから /verify を実行すること。
- SKILL.md の L3 block は Step 5 と Batch Completion Report の 2 箇所に配置されている。review 時に重複感を指摘される可能性があるが、batch mode が Step 5 に到達しないための必要な設計であることを確認。
- rubric AC (AC3) はセマンティック判定なので /verify での再評価が重要。実装内容は rubric text の 5 基準すべてを満たしている。
